spring.application.name=EGA
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:6543/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.vicuxzdqtqhiozotrevg}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:egabackendDJ@bar00}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.data-source-properties.preferQueryMode=simple
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.preparedStatementCacheQueries=0
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:none}
spring.jpa.properties.hibernate.format_sql=true
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=egabankapp@gmail.com
spring.mail.password=ahjw ifvu bkdz zif
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
jwt.secret=cetteCleDoitEtreTresLonguePourEtreSecuriseeEtValidePourHS256_AuMoins32Caracteres
jwt.expiration=900000

# -----------------------------------------------
# CORS — origines autorisées (séparées par des virgules)
# Ajouter l'URL Laravel en production
# -----------------------------------------------
cors.allowed-origins=http://localhost:4200,http://localhost:8000

# -----------------------------------------------
# API Key — clé secrète partagée avec Laravel
# Laravel doit envoyer ce header : X-API-KEY: <valeur>
# -----------------------------------------------
api.secret-key=CLE_DEVELOPPEMENT

# -----------------------------------------------
# Webhook — URL Laravel qui reçoit les notifications
# et secret partagé pour authentifier les appels webhook
# -----------------------------------------------
laravel.webhook.url=http://localhost:8000/api/banking/webhook
laravel.webhook.secret=CHANGER_CE_SECRET_EN_PRODUCTION

# -----------------------------------------------
# Initialisation Super Admin
# -----------------------------------------------
app.superadmin.username=${SUPER_ADMIN_USERNAME:superadmin}
app.superadmin.password=${SUPER_ADMIN_PASSWORD:}
app.superadmin.email=${SUPER_ADMIN_EMAIL:superadmin@ega.com}
app.superadmin.nom=${SUPER_ADMIN_NOM:Super}
app.superadmin.prenom=${SUPER_ADMIN_PRENOM:Admin}
app.superadmin.numero=${SUPER_ADMIN_NUMERO:000000000}

# -----------------------------------------------
# Port Serveur (Requis pour Render)
# -----------------------------------------------
server.port=${PORT:8080}